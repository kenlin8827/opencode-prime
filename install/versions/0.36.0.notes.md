## v0.36.0 — 2026-09-14

[Compare v0.35.0...v0.36.0](https://github.com/kenlin8827/opencode-prime/compare/v0.35.0...v0.36.0)

### 🔎 tgrep search contract

- **Probe-first summary** — the default returns complete `path:count` results with backend and total metadata.
- **Explicit detail modes** — `locations` returns `path:line`; `content` returns `path:line:text`; `files_with_matches` remains a `summary` compatibility alias.
- **Complete and policy-consistent search** — detail modes impose no OCP-only caps, while indexed searches consistently honor configured index path, file-size, and Git requirements.
- **Opt-in request logging** — structured request diagnostics remain disabled unless explicitly enabled.
