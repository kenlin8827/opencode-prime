## v0.38.2 — 2026-09-15

[Compare v0.38.1...v0.38.2](https://github.com/kenlin8827/opencode-prime/compare/v0.38.1...v0.38.2)

### ✨ New preset: qwen-router

- **`providers/qwen-router.json`** — preset for a self-hosted qwen gateway (OpenAI-compatible). Exposes `qwen3.8-flash` and `qwen3.8-max`, both with reasoning + image input. Requires `QWEN_ROUTER_BASE_URL` + `QWEN_ROUTER_API_KEY` environment variables.
- **`profiles/router/qwen.json`** — matching tier profile: flash / standard / pro / vision pinned to `qwen-router/qwen3.8-flash`; only max routes to `qwen-router/qwen3.8-max`.
- Seeded once on upgrade like every preset — existing same-named user files are never overwritten (v0.38.1 seeding logic applies).
